# Operations Runbook

- If import fails: fix source file format and rerun with `--format json`.
- If publish fails: check credentials and deployment status in CLI output.
- If duplicate content warnings appear: adjust template mappings and regenerate.
