# Backlink Opportunities Reference

Seed file: `data/seed/backlinks/100-backlinks.csv`

Use `seo backlink import --source data/seed/backlinks/100-backlinks.csv` to ingest.

## Suggested usage
- Filter by `paid` and `status` for execution planning.
- Prefer `Free` and `Dofollow` entries early for speed.
